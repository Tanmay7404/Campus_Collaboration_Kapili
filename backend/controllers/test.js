const Model = require("../models/chatModel");

class TestController {
    Test(){
        return "API is working !!";
        //Use Async Function/Promise when takes time to store
    }
}

module.exports = TestController;